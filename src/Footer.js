const Footer = () => {
    return(
        <div className="footer">
            <div className="footer-text">Note: Clicking on the correct option will mark it as green (no indication for incorrect option currently)</div>
        </div>
    );
};

export default Footer;